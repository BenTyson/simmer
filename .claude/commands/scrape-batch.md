---
description: Process the scrape queue (batch)
---

Trigger the /api/cron/scrape endpoint to process pending URLs in the queue. The dev server must be running on localhost:3388.

Show the results: how many processed, succeeded, and failed.
